# v3.11.5 (2026-04-26) — device submodule layout

**SMS gateway** is now the standalone repo [esp32-sms-gateway](https://github.com/tinel-c/esp32-sms-gateway), mounted at `device/esp32-sms-gateway`. Policy for future devices: [DEVICE_SUBMODULES.md](../developer/DEVICE_SUBMODULES.md).

**Clone:** `git clone --recurse-submodules …` or `git submodule update --init --recursive`
